{
  "name": "tucu-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "ui/tucu-ui/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project tucu-ui --web",
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["npm install --prefix ui/tucu-ui"]
      }
    }
  }
}
