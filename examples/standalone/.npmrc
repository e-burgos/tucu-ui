# Force local installation, ignore parent workspace
# This ensures node_modules are created locally in this directory
shamefully-hoist=true
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
prefer-workspace-packages=false
